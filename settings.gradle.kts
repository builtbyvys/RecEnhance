pluginManagement {
    repositories {
        google()
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = "RecEnhanced"
include(":android")
include(":shared")
include(":desktop")